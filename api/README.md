# API namespace

[简体中文](README.zh-CN.md)

The runtime currently registers the versioned HTTP API directly from its modules. This
directory is reserved for generated or manually maintained public API schemas as the protocol
grows. See [`docs/protocol.md`](../docs/protocol.md) for the Phase 1 contract.
